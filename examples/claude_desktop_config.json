{
  "mcpServers": {
    "elemm-gateway": {
      "command": "python3",
      "args": [
        "-m",
        "elemm_gateway.cli",
        "http://localhost:8000"
      ],
      "env": {
        "PYTHONPATH": "src",
        "NOTE": "Change 'PYTHONPATH' to the absolute path of the 'src' directory if Claude cannot find the module."
      }
    }
  }
}